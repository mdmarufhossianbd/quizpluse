import Registration from '@/components/Authentication/Registration';

const page = () => {
    return (
        <div>
            <Registration></Registration>
        </div>
    );
};

export default page;